# 배포 URL: 조직/유저가 mealog 일 때 저장소 이름이 mealog.github.io 이어야
# https://mealog.github.io/ (루트). 저장소 이름이 mealog 면 https://mealog.github.io/mealog/ 만 됨.
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  # 배포 중인 실행을 취소하지 않음 (공식 Pages 템플릿과 동일)
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      # GitHub Pages 의 base path 자동 계산
      # - 사용자/조직 페이지(<user>.github.io 저장소)면 "/"
      # - 그 외엔 "/저장소이름/"
      - name: Compute base path
        id: base
        run: |
          REPO_NAME="${GITHUB_REPOSITORY#*/}"
          OWNER="${GITHUB_REPOSITORY%/*}"
          if [ "$REPO_NAME" = "${OWNER}.github.io" ]; then
            echo "base=/" >> "$GITHUB_OUTPUT"
          else
            echo "base=/$REPO_NAME/" >> "$GITHUB_OUTPUT"
          fi

      # GitHub 저장소 Settings → Secrets: VITE_FIREBASE_* (웹 앱 구성 값). 없으면 빌드는 되나 클라우드 로그인 UI는 비활성.
      - name: Build
        env:
          VITE_BASE_PATH: ${{ steps.base.outputs.base }}
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
        run: npm run build

      # 새로고침 시 404 방지 - SPA fallback
      - name: SPA fallback (404.html)
        run: cp dist/index.html dist/404.html

      # configure-pages 는 저장소에 Pages(API)가 아직 없으면 404로 실패한다.
      # Vite 빌드는 아래 outputs가 필요 없으므로 이 단계는 생략한다.
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
